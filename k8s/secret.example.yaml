# EXAMPLE ONLY — never fill in and commit. Create the real Secret per k8s/README.md.
apiVersion: v1
kind: Secret
metadata:
  name: lecho-secrets
  namespace: lecho
type: Opaque
stringData:
  DATABASE_URL: "postgresql+psycopg://user:pass@host:5432/postgres?sslmode=require"
  JWT_SECRET_KEY: "replace-with-a-long-random-value"
  GOOGLE_CLIENT_ID: ""
  AWS_ACCESS_KEY_ID: ""
  AWS_SECRET_ACCESS_KEY: ""
